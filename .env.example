# Copy to .env and fill in. Never commit .env.
ANTHROPIC_API_KEY=
APOLLO_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6
# Optional — enables Hunter.io Email Finder fallback when Apollo returns no email.
HUNTER_API_KEY=
